__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.env
*.local.json
credentials.dev.json

# Console build output. Generated by `cd ui && npm run build`, and by the
# Dockerfile's first stage — committing it would mean the repo and the image
# could disagree about what the console is.
node_modules/
src/ucm_bridge/api/static/
